{
  "name": "tailwindcss-crates",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.8.0",
  "scripts": {
    "build": "cd crates/node && EMNAPI_LINK_DIR=node_modules/emnapi/lib/wasm32-wasip1-threads pnpm build",
    "build:platform": "cd crates/node && pnpm run build:platform",
    "build:wasm": "cd crates/node && EMNAPI_LINK_DIR=node_modules/emnapi/lib/wasm32-wasip1-threads pnpm run build:wasm",
    "test": "cargo test",
    "clean": "cargo clean && cd crates/node && pnpm clean",
    "publish": "pnpm --recursive --filter=\"!@tailwindcss/oxide-wasm32-wasi\" publish --no-git-checks && cd crates/node/npm/wasm32-wasi && pnpm publish --no-git-checks"
  },
  "devDependencies": {
    "pnpm": "^8.0.0"
  }
}